# 02 · Фундамент Windows

## Зачем

`winget` — штатный магазин командных инструментов Windows. Windows Terminal — удобное окно для PowerShell, а `tree.com` уже встроен в систему.

## Шаги

```powershell
powershell -ExecutionPolicy Bypass -File .\02-foundation\install-windows-terminal.ps1
powershell -ExecutionPolicy Bypass -File .\02-foundation\verify.ps1
```

После успешной проверки переходи к `03-git-github/runbook.md`.
